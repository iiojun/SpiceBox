<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "SpiceBox" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <%= stylesheet_link_tag "https://cdn.jsdelivr.net/npm/bootswatch@5.3.3/dist/sandstone/bootstrap.min.css", media: "all", "data-turbo-track": "reload" %>

  </head>

  <body>
    <header>
      <nav class='navbar navbar-light bg-light'>
        <div class='container-fluid'>
          <div class='navbar-brand'>
            <%= link_to (image_tag 'icon.png', size: '36x36'), root_path %>
               SpiceBox
          </div>
          <div class='float-end'>
          <% if user_signed_in? %>
            <%= link_to "#{octicon 'person', height: 24, 
                       class: 'right left', 'aria-label': 'person'}".html_safe,
                 edit_user_registration_path(current_user), 
                 class: 'cursor-pointer' %>
          <% if current_user.is_admin? %>
            <%= link_to "#{octicon 'gear', height: 24,
                       class: 'right left', 'aria-label': 'gear'}".html_safe, admin_root_path, 
                  class: 'cursor-pointer' %>
          <% end %>
            <%= link_to "#{octicon 'sign-out', height: 24, 
                       class: 'right left', 'aria-label': 'sign-out'}".html_safe,
                destroy_user_session_path,
                data: { turbo_method: :delete },
                class: 'cursor-pointer' %>
        <% else %>
            <%= link_to "#{octicon 'sign-in', height: 24, 
                       class: 'right left', 'aria-label': 'sign-in'}".html_safe,
                new_user_session_path,
                class: 'cursor-pointer' %>
            <%= link_to "#{octicon 'person-add', height: 24, 
                       class: 'right left', 'aria-label': 'person-add'}".html_safe,
                       new_user_registration_path,
                class: 'cursor-pointer' %>
        <% end %>
        </div>
      </nav>
    </header>
    <% if notice %>
      <div class='alert alert-success alert-dismissible fade show' role='alert'>
        <%= notice %>
        <button type='button' class='btn-close'
                data-bs-dismiss='alert' aria-label='dismiss'>
        </button>
      </div>
    <% end %>
    <% if alert %>
      <div class='alert alert-warning alert-dismissible fade show' role='alert'>
        <%= alert %>
        <button type='button' class='btn-close'
                data-bs-dismiss='alert' aria-label='dismiss'>
        </button>
      </div>
    <% end %>
    <div class='container mt-3'>
      <%= yield %>
    </div>
  </body>
</html>
